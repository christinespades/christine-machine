# christine-machine
A modular image-processing machine with a minimal black GUI. Plug together reorderable effect boxes, preview results live, then batch-run images through noise, blur, pixelation, halos, glow outlines, masking, background removal, and optional up/downscaling. Supports background processing and folder-based rules.
